{
  "name": "partner-domain-checker-api",
  "version": "1.0.0",
  "description": "API that traces HTTP redirects and checks if the final domain is a known partner domain",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.21.0"
  }
}
